environments {
  default {
  }
  dev {
  }
  uat {
  }
}

webdriver {
  driver = chrome
  autodownload = true

  capabilities {
    "goog:chromeOptions" {
      args = [
        "--remote-allow-origins=*"
        "--incognito"
      ]
    }
  }
}

serenity {
  takeScreenshots = FOR_FAILURES
}
